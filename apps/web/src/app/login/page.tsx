"use client";

import Link from "next/link";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { apiFetch } from "../../lib/api";

export default function Login() { const [register, setRegister] = useState(false); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [name, setName] = useState(""); const [error, setError] = useState<string | null>(null); const router = useRouter(); async function submit(e: React.FormEvent) { e.preventDefault(); setError(null); try { if (register) await apiFetch("/api/auth/register", { method: "POST", body: JSON.stringify({ name, email, password }) }); else await apiFetch("/api/auth/login", { method: "POST", body: JSON.stringify({ email, password }) }); router.push("/"); } catch (err) { setError(err instanceof Error ? err.message : "Could not complete sign in"); } } return <main className="login-page"><section className="login-copy"><Link className="brand" href="/"><span className="brand-mark" /><span className="brand-word">shotlin<span className="brand-dot">.</span></span></Link><div className="login-statement"><h1>Keep the <em>detail.</em></h1><p>A quiet workspace for turning garment references into images you can actually use.</p></div><span className="help" style={{color: "#938a9b"}}>Zero-prompt image generation · v0.1</span></section><section className="login-art"><form className="login-form" onSubmit={submit}><p className="eyebrow">{register ? "Create workspace" : "Welcome back"}</p><h2>{register ? "Start your studio." : "Enter the atelier."}</h2><p>{register ? "Set up a workspace for your team and your references." : "Sign in to pick up where you left off."}</p>{register && <div className="input-field"><label>Name</label><input value={name} onChange={(e) => setName(e.target.value)} placeholder="Your name" /></div>}<div className="input-field"><label>Email</label><input value={email} onChange={(e) => setEmail(e.target.value)} type="email" placeholder="you@studio.com" /></div><div className="input-field"><label>Password</label><input value={password} onChange={(e) => setPassword(e.target.value)} type="password" placeholder="••••••••" /></div>{error && <p className="help" style={{marginTop: 12, color: "var(--coral)"}}>{error}</p>}<button className="button button-coral login-submit">{register ? "Create workspace" : "Sign in"}</button><p className="login-note">{register ? "Already have a workspace? " : "New to Shotlin? "}<button type="button" onClick={() => setRegister(!register)} style={{border: 0, padding: 0, background: "transparent", color: "var(--coral)", fontWeight: 700}}>{register ? "Sign in" : "Create one"}</button></p></form></section></main>; }
